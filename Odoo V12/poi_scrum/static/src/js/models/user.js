odoo.define('poi_scrum.user_model', function (require) {
'use strict';

var Class = require('web.Class');
var rpc = require('web.rpc');


return Class.extend({
    init: function (values) {
        Object.assign(this, values);

    },
});

});